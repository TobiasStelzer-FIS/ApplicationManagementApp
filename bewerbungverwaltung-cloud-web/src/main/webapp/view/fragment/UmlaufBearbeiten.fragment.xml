<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns:commons="sap.ui.commons" xmlns:l="sap.ui.layout" xmlns="sap.m">
	<Dialog title="{i18n>UmlaufBearbeitenTitle}">
		<VBox>
			<Table>
				<headerToolbar>
					<Toolbar>
						<Title text="{i18n>UmlaufDepartments}"/>
					</Toolbar>
				</headerToolbar>
				<columns>
					<Column>
						<header>
							<Label text="{i18n>UmlaufDepartment}"/>
						</header>
					</Column>
					<Column>
						<header>
							<Label text="{i18n>UmlaufSent}"/>
						</header>
					</Column>
					<Column>
						<header>
							<Label text="{i18n>UmlaufRead}"/>
						</header>
					</Column>
					<Column>
						<header>
							<Label text="{i18n>UmlaufStatus}"/>
						</header>
					</Column>
				</columns>
				<ColumnListItem>
					<ObjectIdentifier title="KEP4"/>
					<Label text="{path: 'testModel>EingetragenAm', formatter: '.formatter.formatDatum'}"/>
					<Label text="23.11.2016"/>
					<Label text="Interessiert"/>
				</ColumnListItem>
			</Table>
		</VBox>
		<buttons>
			<Button id="btnUmlaufSave" text="{i18n>DetailSaveButton}" press="onUmlaufSave"/>
			<Button id="btnUmlaufCancel" text="{i18n>DetailCancelButton}" press="onUmlaufCancel"/>
		</buttons>
	</Dialog>
</core:FragmentDefinition>